# ⌨️ مرجع کامل و سریع کلیدهای میانبر

این صفحه کارت مرجع سریع تمام کلیدهای میانبر دسکتاپ، ادیتور Neovim، خط فرمان و ابزارهای سیستم است.

* **`Mod`:** کلید Super (Windows)
* **`<leader>`:** کلید Space

---

## 🖥️ دسکتاپ گرافیکی — Niri

`CapsLock` با `keyd` دو نقش دارد: ضربه‌ی کوتاه `Escape` و نگه‌داشتن `Super/Mod`. بنابراین در همه‌ی ترکیب‌های زیر می‌توان به‌جای کلید Windows، `CapsLock` را نگه داشت.

### راهنما و پنل‌های Noctalia
| کلید | عملکرد |
| :--- | :--- |
| **`Mod + Shift + /`** | نمایش Hotkey Overlay داخلی Niri |
| **`Mod + Space`** | باز و بسته کردن لانچر Noctalia |
| **`Mod + S`** | باز و بسته کردن Control Center |
| **`Mod + ,`** | باز و بسته کردن تنظیمات Noctalia |
| **`Mod + I`** | روشن/خاموش کردن Caffeine برای توقف موقت رفتارهای idle |

### برنامه‌ها و ابزارها
| کلید | عملکرد |
| :--- | :--- |
| **`Mod + Return`** | باز کردن Kitty |
| **`Mod + /`** | باز کردن ترمینال شناور سریع |
| **`Mod + B`** | باز کردن Zen Browser |
| **`Mod + E`** | باز کردن Thunar |
| **`Mod + Shift + E`** | باز کردن Yazi در Kitty |
| **`Mod + V`** | انتخاب از تاریخچه‌ی Cliphist و کپی نتیجه |
| **`Mod + N`** | انتخاب شبکه‌ی Wi-Fi با Fuzzel |
| **`Mod + Alt + C`** | ماشین‌حساب Fuzzel و کپی نتیجه |

### فوکوس، ستون‌ها و پنجره‌ها
| کلید | عملکرد |
| :--- | :--- |
| **`Mod + H` / `L`** یا **`Mod + ←` / `→`** | فوکوس ستون چپ / راست |
| **`Mod + K` / `J`** یا **`Mod + ↑` / `↓`** | فوکوس پنجره‌ی بالا / پایین در ستون |
| **`Mod + Shift + H` / `L`** یا **`Mod + Ctrl + ←` / `→`** | جابه‌جایی ستون به چپ / راست |
| **`Mod + Shift + K` / `J`** یا **`Mod + Ctrl + ↑` / `↓`** | جابه‌جایی پنجره به بالا / پایین در ستون |
| **`Mod + C`** | وسط‌چین کردن ستون جاری |
| **`Mod + W`** | وارد کردن پنجره در ستون کناری |
| **`Mod + Shift + W`** یا **`Mod + .`** | بیرون آوردن پنجره از ستون |
| **`Mod + G`** | تغییر نمایش ستون جاری بین stacked و tabbed |
| **`Mod + T`** | جابه‌جایی پنجره بین tiled و floating |
| **`Mod + Escape`** | تغییر حالت Keyboard Shortcuts Inhibit |

### اندازه و حالت نمایش
| کلید | عملکرد |
| :--- | :--- |
| **`Mod + R`** | چرخش بین عرض‌های ازپیش‌تعریف‌شده‌ی ستون |
| **`Mod + -` / `=`** | کاهش / افزایش عرض ستون به اندازه‌ی ۱۰٪ |
| **`Mod + F`** | بیشینه کردن ستون (Maximize Column) |
| **`Mod + Shift + F`** | تمام‌صفحه‌ی واقعی پنجره (Fullscreen) |
| **`Mod + Ctrl + F`** | تغییر حالت Windowed Fullscreen |

### Overview و Workspaceها
| کلید | عملکرد |
| :--- | :--- |
| **`Mod + Tab`** | باز و بسته کردن Overview |
| **`Mod + Shift + Tab`** | رفتن به Workspace قبلی |
| **`Mod + Wheel Up` / `Down`** | رفتن به Workspace بالا / پایین |
| **`Mod + 1` … `5`** | رفتن مستقیم به Workspace ۱ تا ۵ |
| **`Mod + Shift + 1` … `5`** | انتقال ستون جاری به Workspace ۱ تا ۵ |

### نشست، تصویر و رسانه
| کلید | عملکرد |
| :--- | :--- |
| **`Mod + Q`** | بستن پنجره‌ی جاری |
| **`Mod + Shift + Q`** | خروج از Niri |
| **`Mod + X`** | قفل صفحه با Noctalia |
| **`Mod + Shift + X`** | قفل جایگزین با Swaylock |
| **`Mod + P`** | انتخاب ناحیه و گرفتن Screenshot |
| **`Mod + Shift + P`** | Screenshot از نمایشگر |
| **`Mod + Alt + P`** | Screenshot از پنجره |
| **`Mod + Ctrl + P`** | انتخاب ناحیه و کپی مستقیم تصویر |
| **`Mod + Alt + R`** | شروع/توقف ضبط صفحه |
| **`Mod + Alt + K`** | روشن/خاموش کردن نمایش کلیدهای فشرده‌شده |
| **کلیدهای سخت‌افزاری صدا و روشنایی** | کنترل صدا و روشنایی حتی روی صفحه‌ی قفل |
| **`Mod + Touchpad Scroll Down` / `Up`** | افزایش / کاهش صدا |

---

## 📝 ادیتور — Neovim 0.11+

### ۱. تگل‌های زنده کاربری (`<leader>u` UI Toggles)
| کلید | عملکرد |
| :--- | :--- |
| **`<leader>uz`** | تگل حالت تمرکز (Dim / Focus Scope) |
| **`<leader>uZ`** | تگل حالت ذن (Zen Mode) |
| **`<leader>ui`** | تگل خطوط تورفتگی با هایلایت ۸ سطحی رنگین‌کمانی |
| **`<leader>uN`** | تگل پاپ‌آپ‌های نوتیفیکیشن |
| **`<leader>un`** | بستن و مخفی کردن نوتیفیکیشن‌های جاری |
| **`<leader>uf`** | تگل فرمت خودکار برای فایل/بافر جاری |
| **`<leader>uF`** | تگل فرمت خودکار به صورت سراسری |
| **`<leader>um`** | تگل رندر زنده مارک‌داون |
| **`<leader>ub`** | تگل نمایش نویسنده خط در گیت (Git Blame) |
| **`<leader>ug`** | تگل نمایش خطوط حذف‌شده گیت |
| **`<leader>ul`** | تگل شماره خطوط نسبی / مطلق |
| **`<leader>uW`** | تگل شکست خطوط طولانی (Word Wrap) |
| **`<leader>uS`** | تگل غلط‌یاب املایی (Spell Check) |
| **`<leader>ux`** | تگل سربرگ تابع جاری (Treesitter Context) |

---

### ۲. هوش مصنوعی — CodeCompanion (`<leader>a`)
| کلید | عملکرد |
| :--- | :--- |
| **`<leader>aa`** | باز و بسته کردن پنل چت هوش مصنوعی (Toggle) |
| **`<leader>ai`** | ویرایش و ریفکتور درون متن با پیش‌نمایش Diff |
| **`<leader>ap`** | پالت پرامپت‌ها و اکشن‌های آماده |
| **`<leader>as`** | ارسال متن انتخاب‌شده به چت فعال |
| **`<leader>ag`** | فعال‌سازی حالت Agent با تایید صریح کاربر |
| **`<leader>ah`** | مشاهده تاریخچه گفتگوهای قبلی |
| **`<leader>am`** | انتخاب مدل پیش‌فرض برای چت‌های جدید |
| **`<leader>a?`** | بررسی وضعیت اتصال به درگاه محلی |

---

### ۳. اجرای تعاملی کد و سلول‌های پایتون (`<leader>r`)
| کلید | عملکرد |
| :--- | :--- |
| **`<leader>rp`** | باز کردن پنل IPython در سمت راست tmux |
| **`<leader>rc`** | اجرای سلول پایتون جاری (`# %%`) و ارسال به REPL |
| **`<leader>rl`** | اجرای خط جاری |
| **`<leader>rs`** | اجرای سلکشن در حالت Visual |
| **`<leader>rf`** | اجرای کل فایل جاری در REPL |
| **`<leader>rn` / `]C`** | پرش به سلول بعدی پایتون |
| **`<leader>rN` / `[C`** | پرش به سلول قبلی پایتون |

---

### ۴. جستجوگرها و فایل‌منیجر (Snacks Pickers)
| کلید | عملکرد |
| :--- | :--- |
| **`<leader><space>`** | جستجوی هوشمند ترکیبی (Smart Find) |
| **`<leader>/`** | جستجوی متنی فوق‌سریع در کل سورس‌کد با ripgrep |
| **`<leader>ff`** | یافتن فایل‌ها بر اساس نام |
| **`<leader>fb`** | لیست بافرهای باز |
| **`<leader>ee`** | باز کردن اکسپلورر درختی فایل‌ها |
| **`<leader>gg`** | باز کردن رابط گرافیکی Lazygit |
| **`Alt + i`** | تگل نمایش فایل‌های gitignore درون جستجوگر |
| **`Alt + h`** | تگل نمایش فایل‌های مخفی درون جستجوگر |

---

### ۵. ناوبری زبان‌سرور (LSP) و هوشمندی کد
| کلید | عملکرد |
| :--- | :--- |
| **`gd`** | رفتن به تعریف تابع یا متغیر (Go to Definition) |
| **`gr`** | یافتن تمام ارجاعات در پروژه (References) |
| **`gI`** | رفتن به پیاده‌سازی اینترفیس (Implementations) |
| **`gy`** | رفتن به تعریف Type |
| **`K`** | نمایش مستندات و تایپ در پنجره شناور (Hover) |
| **`<leader>lr`** | تغییر نام متغیر در کل پروژه (Rename) |
| **`<leader>la`** | منوی رفع سریع و کد اکشن‌ها (Code Actions) |
| **`<leader>cf`** | فرمت دستی فایل یا بخش انتخاب‌شده |

---

### ۶. دیباگر تصویری (DAP Suite)
| کلید | عملکرد |
| :--- | :--- |
| **`<leader>db`** | گذاشتن / برداشتن Breakpoint |
| **`<leader>dB`** | گذاشتن Breakpoint شرطی |
| **`<leader>dc`** | ادامه اجرا / شروع دیباگ (Continue) |
| **`<leader>do`** | گام به جلو (Step Over) |
| **`<leader>di`** | گام به داخل تابع (Step Into) |
| **`<leader>dO`** | گام به بیرون تابع (Step Out) |
| **`<leader>du`** | باز و بسته کردن پنل‌های متغیرها و پشته (`dap-ui`) |

---

### ۷. ویرایش سریع با Mini Modules
| کلید | عملکرد |
| :--- | :--- |
| **`gsa` / `gsd` / `gsr`** | افزودن، حذف و جایگزینی پرانتز و کوتیشن (`mini.surround`) |
| **`daf` / `dif`** | حذف کل تابع / حذف بدنه تابع (`mini.ai`) |
| **`vic` / `dac`** | انتخاب بدنه کلاس / حذف کل کلاس (`mini.ai`) |
| **`gcc` / `gc`** | کامنت کردن خط جاری / سلکشن (`mini.comment`) |
| **`<M-j>` / `<M-k>`** | جابه‌جایی فیزیکی خطوط به بالا و پایین (`mini.move`) |

---

### ۸. پرش‌های هوشمند Flash
| کلید | عملکرد |
| :--- | :--- |
| **`s`** | پرش بلادرنگ به هر کلمه یا کاراکتر در صفحه |
| **`S`** | انتخاب ساختاری بر پایه Treesitter |
| **`r`** | انتخاب شیء متنی از راه دور (Remote Flash) |

---

## ⚡ خط فرمان و شل‌ها

| دستور | عملکرد |
| :--- | :--- |
| **`dev`** | نمایش فوری محیط‌ها بدون Flake evaluation |
| **`dev -i`** | انتخاب تعاملی محیط با FZF |
| **`dev <name>`** | ورود به محیط توسعه لحظه‌ای (`agent`, `ai`, `python`, ...) |
| **`dev --shell bash <name>`** | انتخاب Fish، Bash، Zsh یا shell جاری برای ورود تعاملی |
| **`dev --dry-run <name> ...`** | نمایش Flake، root و command نهایی بدون evaluation یا اجرا |
| **`dev -w <name>`** | evaluation صریح و نمایش بسته‌های محیط |
| **`dev --keep <name>`** | refresh کردن GC-root واقعی بدون ورود |
| **`dev --no-keep <name> ...`** | اجرا بدون ایجاد یا refresh کردن root |
| **`dev --roots`** | نمایش وضعیت registration، زمان آخرین استفاده و حجم closure هر root |
| **`dev --prune`** | پاک‌سازی rootهای قدیمی، شکسته و orphan با تأیید |
| **`dev --unkeep <name>`** | حذف حفاظت GC یک محیط یا root orphan |
| **`dev -v / --verbose`** | نمایش جزئیات عملیات Nix و GC-root |
| **`box`** | ورود به `.box/work → /work` با Home ایزوله |
| **`box --dry-run <cmd>`** | نمایش Plan بدون ساخت فایل یا اجرای process |
| **`box --secure <cmd>`** | environment پاک و mountهای محدودتر؛ شبکه فعال می‌ماند |
| **`box --net none <cmd>`** | اجرای بدون شبکه |
| **`box --status`** | نمایش وضعیت و حجم storage محلی Box |
| **`box --inspect <cmd>`** | trace مختصر دسترسی target به مسیرهای کاربر و workspace |
| **`box --inspect-all <cmd>`** | trace کامل و verbose دسترسی فایل‌های target |
| **`box -e <cmd>`** | Home، tmp و work کاملاً موقت |
| **`box --mem 4G <cmd>`** | اجرای دستور با محدودیت ۴ گیگابایت رم |
| **`proxy_on [port]`** | فعال‌سازی پروکسی در تب جاری ترمینال |
| **`nix_proxy [port]`** | فعال‌سازی پروکسی برای بیلد و دانلودهای NixOS |
| **`sw`** | ساخت و فعال‌سازی نسل جدید سیستم‌عامل |
| **`nh clean all --keep 5`** | پاک‌سازی نسل‌های قدیمی و آزادسازی حافظه دیسک |
